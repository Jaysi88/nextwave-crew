import { requireMember } from '@/lib/member';
export async function POST(request:Request){ const member=await requireMember(); if(!member.ok) return member.response; const input=await request.json().catch(()=>({})); const postId=typeof input.postId==='string'?input.postId:''; if(!postId) return Response.json({error:'postId required'},{status:400}); const accessible=await member.sql`select 1 from posts po where po.id=${postId} and exists(select 1 from community_members cm where cm.community_id=po.community_id and cm.user_id=${member.user.id})`; if(!accessible.length) return Response.json({error:'Post unavailable.'},{status:403}); const existing=await member.sql`select 1 from saved_posts where post_id=${postId} and user_id=${member.user.id}`; if(existing.length){ await member.sql`delete from saved_posts where post_id=${postId} and user_id=${member.user.id}`; return Response.json({saved:false}); } await member.sql`insert into saved_posts(post_id,user_id) values (${postId},${member.user.id})`; return Response.json({saved:true}); }
