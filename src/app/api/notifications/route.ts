import { requireMember } from '@/lib/member';
export async function GET(){ const member=await requireMember(); if(!member.ok) return member.response; const rows=await member.sql`select id,type,title,body,action_url,read_at,created_at from notifications where user_id=${member.user.id} order by created_at desc limit 60`; const unread=rows.filter((r)=>!r.read_at).length; return Response.json({notifications:rows,unread}); }
export async function PATCH(request:Request){ const member=await requireMember(); if(!member.ok) return member.response; const input=await request.json().catch(()=>({})); if(input.all===true) await member.sql`update notifications set read_at=coalesce(read_at,now()) where user_id=${member.user.id}`; else if(typeof input.id==='string') await member.sql`update notifications set read_at=coalesce(read_at,now()) where id=${input.id} and user_id=${member.user.id}`; else return Response.json({error:'Notification id required.'},{status:400}); return Response.json({ok:true}); }
